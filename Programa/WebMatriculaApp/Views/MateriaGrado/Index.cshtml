@model IEnumerable<WebMatriculaApp.Entities.MateriaGrado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMateria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMateria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Grado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMateriaGrado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMateriaGrado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMateriaGrado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

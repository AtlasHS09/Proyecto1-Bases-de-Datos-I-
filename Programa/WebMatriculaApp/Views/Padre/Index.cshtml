@model IEnumerable<WebMatriculaApp.Entities.Padre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profesion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conyugue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TelefonoConyugue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPadreNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Profesion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conyugue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelefonoConyugue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPadreNavigation.Cedula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPadre">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPadre">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPadre">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

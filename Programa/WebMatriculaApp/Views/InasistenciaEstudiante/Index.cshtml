@model IEnumerable<WebMatriculaApp.Entities.InasistenciaEstudiante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstudianteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstudianteNavigation.IdEstudiante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdInasistenciaE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdInasistenciaE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdInasistenciaE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
